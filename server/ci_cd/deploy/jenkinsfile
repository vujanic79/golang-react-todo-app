pipeline {
    agent any

    stages {
        stage('Verify Deploy Job') {
            steps {
                script {
                    echo "Job Name: $jobName"
                    echo "Build Number: $buildNumber"
                }
            }
        }
    }
//     test comment
}