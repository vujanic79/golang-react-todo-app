pipeline {
    agent any

    stages {
        stage('Verify Shell Environment') {
            steps {
                script {
                    echo "Job Name: $jobName"
                    echo "Build Number: $buildNumber"
                }
            }
        }
    }
}